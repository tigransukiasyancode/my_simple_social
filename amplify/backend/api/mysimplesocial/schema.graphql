type Post @model
          @auth(rules: [{allow: public, operations: [read, create, delete, update]}])
{
  id: ID!
  content: String!
}

# TODO 1. Add the Comment model with the @model and @auth directives
# TODO 2. run amplify push
# TODO 3. run amplify codegen models